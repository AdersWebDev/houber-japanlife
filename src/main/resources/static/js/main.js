/** 전역 DOM 참조: 카테고리, 인덱싱, 플로팅 등 (각 스크립트에서 존재 여부 확인 후 사용) */
const indexController = document.getElementById('indexing-control');
const indexModal = document.getElementById('indexing-modal');
// const goTop = document.getElementById('go-top');
const categoryBtn = document.getElementById('category');
const categoryModal = document.getElementById('category-box');
const content = document.querySelector('.content');
const floatingDiv = document.getElementById('floating-div');
const consultingBox = document.getElementById('consulting-box');
